#include <stdio.h>
#include "main.h"
/**
 * main - Prints out all arguments received
 * @argc: Number of arguments
 * @argv: Array name
 */
int main (int argc, char *argv[])
{
	int i;

	for(i = 0; i < argc: ++)
	{
		printf("%s\n", argv[i]);
	}

	return (0);
}
