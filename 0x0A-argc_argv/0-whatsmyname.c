#include <stdio.h>
#include "main.h"
/**
 * main - prints the name of the program file
 * @argc: argument count
 * @argv: argument vector
 * return: 0 (succes)
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
